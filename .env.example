# Server port (Railway injects this automatically)
PORT=4301

# CORS origin (default: '*'; comma-separated for multiple origins)
CORS_ORIGIN=*

# Path to client dist folder (default: packages/client/dist relative to server source)
CLIENT_DIST_PATH=

# Client: Socket.IO server URL (empty = same origin, which works in production)
VITE_SERVER_URL=
